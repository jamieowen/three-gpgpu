{
  "name": "examples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "basic": "NODE_PATH=./node_modules budo src/basic.js",
    "bundle": "browserify src/basic.js -o bundle.js -t babelify"
  },
  "author": "Jamie Owen <hello@jamieowen.com> (=)",
  "license": "ISC",
  "dependencies": {
    "three": "^0.87.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.0.0-beta.32",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^8.0.0",
    "browserify": "^14.5.0",
    "budo": "^10.0.4"
  },
  "babel": {
    "plugins": [],
    "presets": [
      "es2015"
    ]
  },
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  }
}
